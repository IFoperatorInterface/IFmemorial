enum SoundMode {
  SINGLE, CHORD, RANDOM
}


enum BarMode {
  BOUNCE, BLINK, STRETCH
}


enum FieldMode {
  UP, DOWN, LEFT, RIGHT, ELLIPSE
}


enum Rgb {
  RED, GREEN, BLUE
}


enum Log {
  TOUCH, MOVE, PULL, JUMP, TRIGGER, RECORD_START, RECORD_END, RECORD_PLAY, RECORD_STOP
}
