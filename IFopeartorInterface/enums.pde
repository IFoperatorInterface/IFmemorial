enum SoundMode {
  SINGLE, CHORD, RANDOM
}


enum BarMode {
  BOUNCE, BLINK, STRETCH
}


enum FieldMode {
  UP, DOWN, LEFT, RIGHT, ELLIPSE
}


enum Rgb {
  RED, GREEN, BLUE
}


enum Preset {
  TOUCH, JUMP
}
